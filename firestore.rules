rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Secure rule: Users can only read and write their own receivers directory records
    match /users/{userId}/receivers/{receiverId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
